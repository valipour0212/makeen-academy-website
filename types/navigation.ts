export interface HeaderNavItemType {
    id: string;
    label: string;
    href: string;
}

export interface ButtonNavItemType {
    id: string;
    label: string;
    href: string;
    icon: string;
    activeIcon: string;
}